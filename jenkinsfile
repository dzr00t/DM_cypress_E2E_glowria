pipeline {
    agent {
        docker{
            image 'cypress/included:14.5.4'
            args '--entrypoint='
        }
    }

    stages {
        stage('instalation dependance') {
            steps {
                sh 'npm ci'
            }
        }


            stage('lancement de test') {
            steps {
                sh 'npx cypress run'
            }
        }
    }
}
